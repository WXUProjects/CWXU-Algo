# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Contest API
    description: 竞赛服务
    version: 0.0.1
paths:
    /v1/core/contest/history:
        get:
            tags:
                - Contest
            description: 获取用户竞赛历史
            operationId: Contest_GetUserContestHistory
            parameters:
                - name: userId
                  in: query
                  schema:
                    type: string
                - name: limit
                  in: query
                  schema:
                    type: string
                - name: cursor
                  in: query
                  schema:
                    type: string
                - name: platform
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetUserContestHistoryRes'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/core/contest/list:
        get:
            tags:
                - Contest
            description: 获取竞赛列表
            operationId: Contest_GetContestList
            parameters:
                - name: userId
                  in: query
                  schema:
                    type: string
                - name: limit
                  in: query
                  schema:
                    type: string
                - name: offset
                  in: query
                  schema:
                    type: string
                - name: platform
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetContestListRes'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/core/contest/ranking:
        get:
            tags:
                - Contest
            description: 获取竞赛排行榜
            operationId: Contest_GetContestRanking
            parameters:
                - name: contestId
                  in: query
                  schema:
                    type: string
                - name: groupId
                  in: query
                  schema:
                    type: string
                - name: limit
                  in: query
                  schema:
                    type: string
                - name: offset
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetContestRankingRes'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        ContestLog:
            type: object
            properties:
                id:
                    type: integer
                    format: uint32
                platform:
                    type: string
                contestId:
                    type: string
                contestName:
                    type: string
                contestUrl:
                    type: string
                totalCount:
                    type: integer
                    format: int32
                time:
                    type: string
            description: 竞赛日志消息体
        GetContestListRes:
            type: object
            properties:
                code:
                    type: string
                message:
                    type: string
                data:
                    type: array
                    items:
                        $ref: '#/components/schemas/ContestLog'
                total:
                    type: string
            description: 获取竞赛列表响应
        GetContestRankingRes:
            type: object
            properties:
                code:
                    type: string
                message:
                    type: string
                contest:
                    $ref: '#/components/schemas/ContestLog'
                data:
                    type: array
                    items:
                        $ref: '#/components/schemas/RankingItem'
                total:
                    type: string
            description: 获取竞赛排行榜响应
        GetUserContestHistoryRes:
            type: object
            properties:
                code:
                    type: string
                message:
                    type: string
                data:
                    type: array
                    items:
                        $ref: '#/components/schemas/ContestLog'
            description: 获取用户竞赛历史响应
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        RankingItem:
            type: object
            properties:
                rank:
                    type: string
                userId:
                    type: string
                name:
                    type: string
                score:
                    type: integer
                    format: int32
                acCount:
                    type: integer
                    format: int32
                totalCount:
                    type: integer
                    format: int32
            description: 排行榜项
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Contest
