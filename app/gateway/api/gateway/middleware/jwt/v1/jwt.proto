syntax = "proto3";

package gateway.middleware.jwt.v1;

option go_package = "github.com/go-kratos/gateway/api/gateway/middleware/jwt/v1;jwtv1";

message JWT {
  // Must match server.jwt_secret in user/core_data/agent config.yaml.
  string secret = 1;
}
